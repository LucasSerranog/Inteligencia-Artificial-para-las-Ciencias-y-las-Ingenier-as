answer-0202 — usecase generator error
El error reportado es:
marcar_anomalias() missing 2 required positional arguments: 'df' and 'umbral'
Este error proviene del generador de caso de uso, específicamente porque en sus líneas 13-17 el generador realiza los cálculos directamente sobre el DataFrame sin invocar la función implementada por el estudiante (calcular_rotacion_inventario(df)). Al no llamar correctamente la función del estudiante con los argumentos requeridos, el sistema lanza un error de ejecución que es ajeno a la solución presentada.
La solución implementada en answer-0202.py define correctamente la función solicitada, pero el generador nunca la invoca con los parámetros necesarios, lo que genera el fallo en tiempo de ejecución.
